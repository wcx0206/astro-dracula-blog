---
const { tag, count } = Astro.props;

let text = tag;
if (count && count > 1) {
  text = `${tag} (${count})`;
}
---

<code
  class="inline-block px-2 py-1 text-dracula-purple bg-dracula-dark/80 hover:bg-dracula-dark transition"
  >{text}</code
>
